# r003_ac_pip

所属系统：可调节情绪系统 (`modifiable_emotion`)

分入原因：情绪生成有 BF 支持，且重评方向表现为下降调节；用于定位可被重评改变的情绪反应区域。

该 ROI 来自 `after_cluster` 图 `PIP` 的非重叠三维连通簇。
体素数：43；体素坐标质心：(52.47, 53.47, 64.07)。

支撑数据：

- `mask.nii.gz`：该 ROI 的脑图。
- `roi_summary.csv`：体素数、质心、来源系统图和重叠移除信息。
