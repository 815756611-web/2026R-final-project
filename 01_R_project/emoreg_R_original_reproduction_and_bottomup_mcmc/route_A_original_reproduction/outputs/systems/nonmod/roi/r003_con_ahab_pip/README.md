# r003_con_ahab_pip

所属系统：不可调节情绪系统 (`non_modifiable_emotion`)

分入原因：情绪生成有 BF 支持，同时有证据不支持重评调节；用于定位较难被重评改变的情绪反应区域。

该 ROI 来自 `consensus` 图 `AHAB_PIP` 的非重叠三维连通簇。
体素数：45；体素坐标质心：(44.09, 71.22, 71.29)。

支撑数据：

- `mask.nii.gz`：该 ROI 的脑图。
- `roi_summary.csv`：体素数、质心、来源系统图和重叠移除信息。
