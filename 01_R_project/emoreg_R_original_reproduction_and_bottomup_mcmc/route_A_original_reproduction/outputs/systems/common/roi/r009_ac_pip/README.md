# r009_ac_pip

所属系统：共同评价系统 (`common_appraisal`)

分入原因：情绪生成和重评效应同时有 BF 支持；用于定位两种过程共享的评价相关区域。

该 ROI 来自 `after_cluster` 图 `PIP` 的非重叠三维连通簇。
体素数：91；体素坐标质心：(64.6, 26.51, 41.92)。

支撑数据：

- `mask.nii.gz`：该 ROI 的脑图。
- `roi_summary.csv`：体素数、质心、来源系统图和重叠移除信息。
