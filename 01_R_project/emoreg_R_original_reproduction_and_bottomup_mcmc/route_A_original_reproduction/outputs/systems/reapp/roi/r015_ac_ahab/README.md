# r015_ac_ahab

所属系统：重评专属系统 (`reappraisal_only`)

分入原因：重评效应有 BF 支持，同时情绪生成没有稳定 BF 支持；用于定位更偏重认知重评的区域。

该 ROI 来自 `after_cluster` 图 `AHAB` 的非重叠三维连通簇。
体素数：33；体素坐标质心：(40.79, 32.42, 28.18)。

支撑数据：

- `mask.nii.gz`：该 ROI 的脑图。
- `roi_summary.csv`：体素数、质心、来源系统图和重叠移除信息。
